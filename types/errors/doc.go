// Package errors 定义 API 和业务错误码契约以及业务错误类型。
//
// 边界说明:
// - 错误码按范围分段,供 HTTP 响应、前端和日志统一识别错误类型。
// - 3000-3999 是认证/授权预留范围,但当前项目未实现 auth/rbac。
// - 本包只定义错误码和 BizError,不负责认证、授权、token 校验或 HTTP 写响应。
package errors

// 本文件承载包级 Godoc 入口，集中说明该包在脚手架架构中的定位、使用边界和非目标能力。
