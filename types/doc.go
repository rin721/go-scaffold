// Package types 提供项目级跨层契约入口。
//
// 边界说明:
// - 根 types 包只保留应用层以上可共享的项目契约说明,不向 pkg/* 基础设施包提供别名或注入接口。
// - 需要加密或缓存能力的应用层/组合层代码应显式依赖对应 pkg 包,或在应用层以上另行定义本层契约。
// - 本包不是业务领域模型集合,不承载 HTTP 响应 helper、数据库模型、认证实现或基础设施聚合入口。
// - 新增根 types 类型前应先确认它是否属于应用层以上的跨层契约。
package types

// 本文件承载包级 Godoc 入口，集中说明该包在脚手架架构中的定位、使用边界和非目标能力。
