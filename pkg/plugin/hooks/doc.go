// Package hooks provides a small typed hook engine for plugin runtimes.
//
// The package is deliberately independent from application infrastructure. It
// does not import logger, config, IAM, or internal packages.
package hooks
